<!-- Autogenerated by mlir-tblgen; don't manually edit -->
# 'scope' Dialect Passes

## `-inline-scope`

_内联（Inline）ScopeOp 中的 scope 区域。_

如果 `scope.scope` 没有 `no_inline` 属性，则将其区域内部的操作移动到父区域中。
示例：

```mlir
func.func @test() {
  scope.scope : () -> () {
    ...
    scope.return
  } {no_inline}
  scope.scope : () -> () {
    <inlinable_operations>
    scope.return
  }
  return
}
```

将被转换为：

```mlir
func.func @test() {
  scope.scope : () -> () {
    ...
    scope.return
  } {no_inline}
  <inlinable_operations>
  return
}
```

### 选项（Options）

```text
-force-inline : 忽略 `no_inline` 属性，强制内联 scope。
```

## `-outline-scope`

_将 ScopeOp 中的 scope 区域外提（Outline）。_

将 `scope.scope` 转换为 `func.func`。

示例：

```mlir
module {
  func.func @test() {
    scope.scope : () -> () {
      ...
      scope.return
    } {tcore_type = #hivm.tcore_type<CUBE>, ...}
    scope.scope : () -> () {
      ...
      scope.return
    } {tcore_type = #hivm.tcore_type<VECTOR>, ...}
    return
  }
}
```

将被转换为：

```mlir
module {
  func.func @test_scope_0() attributes {tcore_type = #hivm.tcore_type<CUBE>, ...} {
    ...
    return
  }
  func.func @test_scope_1() attributes {tcore_type = #hivm.tcore_type<VECTOR>, ...} {
    ...
    return
  }
  func.func @test() {
    call @test_scope_scope_scope_0() : () -> ()
    call @test_scope_scope_scope_1() : () -> ()
    return
  }
}
```
